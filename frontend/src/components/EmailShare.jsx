const EmailShare = () => {
  return (
    <section>
      <h2>Email Share</h2>
      <input type="text" placeholder="Enter email addresses" />
      <button>Share</button>
    </section>
  );
};

export default EmailShare;