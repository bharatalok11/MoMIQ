const PromptInput = () => {
  return (
    <section>
      <h2>Custom Instructions</h2>
      <textarea rows="3" placeholder="e.g., 'Summarize in bullet points for executives' or 'Highlight only action items'" />
    </section>
  );
};

export default PromptInput;